# {py:mod}`duck.utils.slug`

```{py:module} duck.utils.slug
```

```{autodocx-docstring} duck.utils.slug
:allowtitles:
```

## Module Contents

### Functions

````{list-table}
:class: autosummary longtable
:align: left

* - {py:obj}`clean_slug <duck.utils.slug.clean_slug>`
  - ```{autodocx-docstring} duck.utils.slug.clean_slug
    :summary:
    ```
* - {py:obj}`generate_slug_from_string <duck.utils.slug.generate_slug_from_string>`
  - ```{autodocx-docstring} duck.utils.slug.generate_slug_from_string
    :summary:
    ```
* - {py:obj}`is_valid_slug <duck.utils.slug.is_valid_slug>`
  - ```{autodocx-docstring} duck.utils.slug.is_valid_slug
    :summary:
    ```
* - {py:obj}`join_slug <duck.utils.slug.join_slug>`
  - ```{autodocx-docstring} duck.utils.slug.join_slug
    :summary:
    ```
* - {py:obj}`sanitize_slug <duck.utils.slug.sanitize_slug>`
  - ```{autodocx-docstring} duck.utils.slug.sanitize_slug
    :summary:
    ```
* - {py:obj}`slugify <duck.utils.slug.slugify>`
  - ```{autodocx-docstring} duck.utils.slug.slugify
    :summary:
    ```
* - {py:obj}`split_slug <duck.utils.slug.split_slug>`
  - ```{autodocx-docstring} duck.utils.slug.split_slug
    :summary:
    ```
* - {py:obj}`truncate_slug <duck.utils.slug.truncate_slug>`
  - ```{autodocx-docstring} duck.utils.slug.truncate_slug
    :summary:
    ```
* - {py:obj}`unslugify <duck.utils.slug.unslugify>`
  - ```{autodocx-docstring} duck.utils.slug.unslugify
    :summary:
    ```
````

### API

````{py:function} clean_slug(slug: str, separator: str = '-') -> str
:canonical: duck.utils.slug.clean_slug

```{autodocx-docstring} duck.utils.slug.clean_slug
```
````

````{py:function} generate_slug_from_string(text: str, separator: str = '-') -> str
:canonical: duck.utils.slug.generate_slug_from_string

```{autodocx-docstring} duck.utils.slug.generate_slug_from_string
```
````

````{py:function} is_valid_slug(slug: str, separator: str = '-') -> bool
:canonical: duck.utils.slug.is_valid_slug

```{autodocx-docstring} duck.utils.slug.is_valid_slug
```
````

````{py:function} join_slug(words: list, separator: str = '-') -> str
:canonical: duck.utils.slug.join_slug

```{autodocx-docstring} duck.utils.slug.join_slug
```
````

````{py:function} sanitize_slug(slug: str, separator: str = '-') -> str
:canonical: duck.utils.slug.sanitize_slug

```{autodocx-docstring} duck.utils.slug.sanitize_slug
```
````

````{py:function} slugify(text: str, separator: str = '-') -> str
:canonical: duck.utils.slug.slugify

```{autodocx-docstring} duck.utils.slug.slugify
```
````

````{py:function} split_slug(slug: str, separator: str = '-') -> list
:canonical: duck.utils.slug.split_slug

```{autodocx-docstring} duck.utils.slug.split_slug
```
````

````{py:function} truncate_slug(slug: str, max_length: int, separator: str = '-') -> str
:canonical: duck.utils.slug.truncate_slug

```{autodocx-docstring} duck.utils.slug.truncate_slug
```
````

````{py:function} unslugify(slug: str, separator: str = '-') -> str
:canonical: duck.utils.slug.unslugify

```{autodocx-docstring} duck.utils.slug.unslugify
```
````
