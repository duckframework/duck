# {py:mod}`duck.utils.string`

```{py:module} duck.utils.string
```

```{autodocx-docstring} duck.utils.string
:allowtitles:
```

## Module Contents

### Functions

````{list-table}
:class: autosummary longtable
:align: left

* - {py:obj}`are_anagrams <duck.utils.string.are_anagrams>`
  - ```{autodocx-docstring} duck.utils.string.are_anagrams
    :summary:
    ```
* - {py:obj}`camel_to_snake_case <duck.utils.string.camel_to_snake_case>`
  - ```{autodocx-docstring} duck.utils.string.camel_to_snake_case
    :summary:
    ```
* - {py:obj}`capitalize_words <duck.utils.string.capitalize_words>`
  - ```{autodocx-docstring} duck.utils.string.capitalize_words
    :summary:
    ```
* - {py:obj}`clean_string <duck.utils.string.clean_string>`
  - ```{autodocx-docstring} duck.utils.string.clean_string
    :summary:
    ```
* - {py:obj}`count_words <duck.utils.string.count_words>`
  - ```{autodocx-docstring} duck.utils.string.count_words
    :summary:
    ```
* - {py:obj}`find_all_occurrences <duck.utils.string.find_all_occurrences>`
  - ```{autodocx-docstring} duck.utils.string.find_all_occurrences
    :summary:
    ```
* - {py:obj}`find_and_replace <duck.utils.string.find_and_replace>`
  - ```{autodocx-docstring} duck.utils.string.find_and_replace
    :summary:
    ```
* - {py:obj}`find_occurrences <duck.utils.string.find_occurrences>`
  - ```{autodocx-docstring} duck.utils.string.find_occurrences
    :summary:
    ```
* - {py:obj}`from_base64 <duck.utils.string.from_base64>`
  - ```{autodocx-docstring} duck.utils.string.from_base64
    :summary:
    ```
* - {py:obj}`is_camel_case <duck.utils.string.is_camel_case>`
  - ```{autodocx-docstring} duck.utils.string.is_camel_case
    :summary:
    ```
* - {py:obj}`is_kebab_case <duck.utils.string.is_kebab_case>`
  - ```{autodocx-docstring} duck.utils.string.is_kebab_case
    :summary:
    ```
* - {py:obj}`is_palindrome <duck.utils.string.is_palindrome>`
  - ```{autodocx-docstring} duck.utils.string.is_palindrome
    :summary:
    ```
* - {py:obj}`is_pascal_case <duck.utils.string.is_pascal_case>`
  - ```{autodocx-docstring} duck.utils.string.is_pascal_case
    :summary:
    ```
* - {py:obj}`is_screaming_snake_case <duck.utils.string.is_screaming_snake_case>`
  - ```{autodocx-docstring} duck.utils.string.is_screaming_snake_case
    :summary:
    ```
* - {py:obj}`is_snake_case <duck.utils.string.is_snake_case>`
  - ```{autodocx-docstring} duck.utils.string.is_snake_case
    :summary:
    ```
* - {py:obj}`is_train_case <duck.utils.string.is_train_case>`
  - ```{autodocx-docstring} duck.utils.string.is_train_case
    :summary:
    ```
* - {py:obj}`justify_text <duck.utils.string.justify_text>`
  - ```{autodocx-docstring} duck.utils.string.justify_text
    :summary:
    ```
* - {py:obj}`pad_text <duck.utils.string.pad_text>`
  - ```{autodocx-docstring} duck.utils.string.pad_text
    :summary:
    ```
* - {py:obj}`re_remove_non_alphanumeric <duck.utils.string.re_remove_non_alphanumeric>`
  - ```{autodocx-docstring} duck.utils.string.re_remove_non_alphanumeric
    :summary:
    ```
* - {py:obj}`remove_extra_spaces <duck.utils.string.remove_extra_spaces>`
  - ```{autodocx-docstring} duck.utils.string.remove_extra_spaces
    :summary:
    ```
* - {py:obj}`remove_non_alphanumeric <duck.utils.string.remove_non_alphanumeric>`
  - ```{autodocx-docstring} duck.utils.string.remove_non_alphanumeric
    :summary:
    ```
* - {py:obj}`remove_punctuation <duck.utils.string.remove_punctuation>`
  - ```{autodocx-docstring} duck.utils.string.remove_punctuation
    :summary:
    ```
* - {py:obj}`replace_multiple <duck.utils.string.replace_multiple>`
  - ```{autodocx-docstring} duck.utils.string.replace_multiple
    :summary:
    ```
* - {py:obj}`reverse_words_in_sentence <duck.utils.string.reverse_words_in_sentence>`
  - ```{autodocx-docstring} duck.utils.string.reverse_words_in_sentence
    :summary:
    ```
* - {py:obj}`smart_truncate <duck.utils.string.smart_truncate>`
  - ```{autodocx-docstring} duck.utils.string.smart_truncate
    :summary:
    ```
* - {py:obj}`snake_to_camel_case <duck.utils.string.snake_to_camel_case>`
  - ```{autodocx-docstring} duck.utils.string.snake_to_camel_case
    :summary:
    ```
* - {py:obj}`string_similarity <duck.utils.string.string_similarity>`
  - ```{autodocx-docstring} duck.utils.string.string_similarity
    :summary:
    ```
* - {py:obj}`to_base64 <duck.utils.string.to_base64>`
  - ```{autodocx-docstring} duck.utils.string.to_base64
    :summary:
    ```
* - {py:obj}`to_camel_case <duck.utils.string.to_camel_case>`
  - ```{autodocx-docstring} duck.utils.string.to_camel_case
    :summary:
    ```
* - {py:obj}`to_kebab_case <duck.utils.string.to_kebab_case>`
  - ```{autodocx-docstring} duck.utils.string.to_kebab_case
    :summary:
    ```
* - {py:obj}`to_spaced_camel_case <duck.utils.string.to_spaced_camel_case>`
  - ```{autodocx-docstring} duck.utils.string.to_spaced_camel_case
    :summary:
    ```
* - {py:obj}`transform_case_based_on_condition <duck.utils.string.transform_case_based_on_condition>`
  - ```{autodocx-docstring} duck.utils.string.transform_case_based_on_condition
    :summary:
    ```
* - {py:obj}`wrap_text <duck.utils.string.wrap_text>`
  - ```{autodocx-docstring} duck.utils.string.wrap_text
    :summary:
    ```
* - {py:obj}`wrap_words <duck.utils.string.wrap_words>`
  - ```{autodocx-docstring} duck.utils.string.wrap_words
    :summary:
    ```
````

### API

````{py:function} are_anagrams(str1: str, str2: str) -> bool
:canonical: duck.utils.string.are_anagrams

```{autodocx-docstring} duck.utils.string.are_anagrams
```
````

````{py:function} camel_to_snake_case(text: str) -> str
:canonical: duck.utils.string.camel_to_snake_case

```{autodocx-docstring} duck.utils.string.camel_to_snake_case
```
````

````{py:function} capitalize_words(text: str) -> str
:canonical: duck.utils.string.capitalize_words

```{autodocx-docstring} duck.utils.string.capitalize_words
```
````

````{py:function} clean_string(text: str, remove_chars: list[str] = None) -> str
:canonical: duck.utils.string.clean_string

```{autodocx-docstring} duck.utils.string.clean_string
```
````

````{py:function} count_words(text: str) -> int
:canonical: duck.utils.string.count_words

```{autodocx-docstring} duck.utils.string.count_words
```
````

````{py:function} find_all_occurrences(text: str, target: str) -> typing.List[int]
:canonical: duck.utils.string.find_all_occurrences

```{autodocx-docstring} duck.utils.string.find_all_occurrences
```
````

````{py:function} find_and_replace(text: str, target: str, replacement: str) -> str
:canonical: duck.utils.string.find_and_replace

```{autodocx-docstring} duck.utils.string.find_and_replace
```
````

````{py:function} find_occurrences(text: str, substring: str) -> list
:canonical: duck.utils.string.find_occurrences

```{autodocx-docstring} duck.utils.string.find_occurrences
```
````

````{py:function} from_base64(encoded_text: str) -> str
:canonical: duck.utils.string.from_base64

```{autodocx-docstring} duck.utils.string.from_base64
```
````

````{py:function} is_camel_case(s: str) -> bool
:canonical: duck.utils.string.is_camel_case

```{autodocx-docstring} duck.utils.string.is_camel_case
```
````

````{py:function} is_kebab_case(s: str) -> bool
:canonical: duck.utils.string.is_kebab_case

```{autodocx-docstring} duck.utils.string.is_kebab_case
```
````

````{py:function} is_palindrome(text: str) -> bool
:canonical: duck.utils.string.is_palindrome

```{autodocx-docstring} duck.utils.string.is_palindrome
```
````

````{py:function} is_pascal_case(s: str) -> bool
:canonical: duck.utils.string.is_pascal_case

```{autodocx-docstring} duck.utils.string.is_pascal_case
```
````

````{py:function} is_screaming_snake_case(s: str) -> bool
:canonical: duck.utils.string.is_screaming_snake_case

```{autodocx-docstring} duck.utils.string.is_screaming_snake_case
```
````

````{py:function} is_snake_case(s: str) -> bool
:canonical: duck.utils.string.is_snake_case

```{autodocx-docstring} duck.utils.string.is_snake_case
```
````

````{py:function} is_train_case(s: str) -> bool
:canonical: duck.utils.string.is_train_case

```{autodocx-docstring} duck.utils.string.is_train_case
```
````

````{py:function} justify_text(text: str, width: int, direction: str = 'left') -> str
:canonical: duck.utils.string.justify_text

```{autodocx-docstring} duck.utils.string.justify_text
```
````

````{py:function} pad_text(text: str, total_length: int, padding_char: str = ' ') -> str
:canonical: duck.utils.string.pad_text

```{autodocx-docstring} duck.utils.string.pad_text
```
````

````{py:function} re_remove_non_alphanumeric(text: str) -> str
:canonical: duck.utils.string.re_remove_non_alphanumeric

```{autodocx-docstring} duck.utils.string.re_remove_non_alphanumeric
```
````

````{py:function} remove_extra_spaces(text: str) -> str
:canonical: duck.utils.string.remove_extra_spaces

```{autodocx-docstring} duck.utils.string.remove_extra_spaces
```
````

````{py:function} remove_non_alphanumeric(text: str) -> str
:canonical: duck.utils.string.remove_non_alphanumeric

```{autodocx-docstring} duck.utils.string.remove_non_alphanumeric
```
````

````{py:function} remove_punctuation(text: str) -> str
:canonical: duck.utils.string.remove_punctuation

```{autodocx-docstring} duck.utils.string.remove_punctuation
```
````

````{py:function} replace_multiple(text: str, replacements: dict) -> str
:canonical: duck.utils.string.replace_multiple

```{autodocx-docstring} duck.utils.string.replace_multiple
```
````

````{py:function} reverse_words_in_sentence(text: str) -> str
:canonical: duck.utils.string.reverse_words_in_sentence

```{autodocx-docstring} duck.utils.string.reverse_words_in_sentence
```
````

````{py:function} smart_truncate(text: str, cap: int, grammar: list = None, suffix: str = '...') -> str
:canonical: duck.utils.string.smart_truncate

```{autodocx-docstring} duck.utils.string.smart_truncate
```
````

````{py:function} snake_to_camel_case(text: str) -> str
:canonical: duck.utils.string.snake_to_camel_case

```{autodocx-docstring} duck.utils.string.snake_to_camel_case
```
````

````{py:function} string_similarity(str1: str, str2: str) -> float
:canonical: duck.utils.string.string_similarity

```{autodocx-docstring} duck.utils.string.string_similarity
```
````

````{py:function} to_base64(text: str) -> str
:canonical: duck.utils.string.to_base64

```{autodocx-docstring} duck.utils.string.to_base64
```
````

````{py:function} to_camel_case(text: str, separator: str = '_') -> str
:canonical: duck.utils.string.to_camel_case

```{autodocx-docstring} duck.utils.string.to_camel_case
```
````

````{py:function} to_kebab_case(text: str) -> str
:canonical: duck.utils.string.to_kebab_case

```{autodocx-docstring} duck.utils.string.to_kebab_case
```
````

````{py:function} to_spaced_camel_case(value: str) -> str
:canonical: duck.utils.string.to_spaced_camel_case

```{autodocx-docstring} duck.utils.string.to_spaced_camel_case
```
````

````{py:function} transform_case_based_on_condition(text: str, condition: bool) -> str
:canonical: duck.utils.string.transform_case_based_on_condition

```{autodocx-docstring} duck.utils.string.transform_case_based_on_condition
```
````

````{py:function} wrap_text(text: str, width: int, align: str = 'left') -> str
:canonical: duck.utils.string.wrap_text

```{autodocx-docstring} duck.utils.string.wrap_text
```
````

````{py:function} wrap_words(text: str, line_length: int) -> str
:canonical: duck.utils.string.wrap_words

```{autodocx-docstring} duck.utils.string.wrap_words
```
````
