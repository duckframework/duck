# {py:mod}`duck.utils.extraction`

```{py:module} duck.utils.extraction
```

```{autodocx-docstring} duck.utils.extraction
:allowtitles:
```

## Module Contents

### Functions

````{list-table}
:class: autosummary longtable
:align: left

* - {py:obj}`extract_currency <duck.utils.extraction.extract_currency>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_currency
    :summary:
    ```
* - {py:obj}`extract_dates <duck.utils.extraction.extract_dates>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_dates
    :summary:
    ```
* - {py:obj}`extract_emails <duck.utils.extraction.extract_emails>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_emails
    :summary:
    ```
* - {py:obj}`extract_hashtags <duck.utils.extraction.extract_hashtags>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_hashtags
    :summary:
    ```
* - {py:obj}`extract_hex_colors <duck.utils.extraction.extract_hex_colors>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_hex_colors
    :summary:
    ```
* - {py:obj}`extract_ips <duck.utils.extraction.extract_ips>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_ips
    :summary:
    ```
* - {py:obj}`extract_mentions <duck.utils.extraction.extract_mentions>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_mentions
    :summary:
    ```
* - {py:obj}`extract_phone_numbers <duck.utils.extraction.extract_phone_numbers>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_phone_numbers
    :summary:
    ```
* - {py:obj}`extract_skus <duck.utils.extraction.extract_skus>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_skus
    :summary:
    ```
* - {py:obj}`extract_social_handles <duck.utils.extraction.extract_social_handles>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_social_handles
    :summary:
    ```
* - {py:obj}`extract_urls <duck.utils.extraction.extract_urls>`
  - ```{autodocx-docstring} duck.utils.extraction.extract_urls
    :summary:
    ```
````

### API

````{py:function} extract_currency(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_currency

```{autodocx-docstring} duck.utils.extraction.extract_currency
```
````

````{py:function} extract_dates(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_dates

```{autodocx-docstring} duck.utils.extraction.extract_dates
```
````

````{py:function} extract_emails(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_emails

```{autodocx-docstring} duck.utils.extraction.extract_emails
```
````

````{py:function} extract_hashtags(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_hashtags

```{autodocx-docstring} duck.utils.extraction.extract_hashtags
```
````

````{py:function} extract_hex_colors(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_hex_colors

```{autodocx-docstring} duck.utils.extraction.extract_hex_colors
```
````

````{py:function} extract_ips(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_ips

```{autodocx-docstring} duck.utils.extraction.extract_ips
```
````

````{py:function} extract_mentions(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_mentions

```{autodocx-docstring} duck.utils.extraction.extract_mentions
```
````

````{py:function} extract_phone_numbers(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_phone_numbers

```{autodocx-docstring} duck.utils.extraction.extract_phone_numbers
```
````

````{py:function} extract_skus(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_skus

```{autodocx-docstring} duck.utils.extraction.extract_skus
```
````

````{py:function} extract_social_handles(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_social_handles

```{autodocx-docstring} duck.utils.extraction.extract_social_handles
```
````

````{py:function} extract_urls(text: str) -> typing.List[str]
:canonical: duck.utils.extraction.extract_urls

```{autodocx-docstring} duck.utils.extraction.extract_urls
```
````
