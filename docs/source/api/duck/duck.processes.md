# {py:mod}`duck.processes`

```{py:module} duck.processes
```

```{autodocx-docstring} duck.processes
:allowtitles:
```

## Module Contents

### Functions

````{list-table}
:class: autosummary longtable
:align: left

* - {py:obj}`clear_processes_data <duck.processes.clear_processes_data>`
  - ```{autodocx-docstring} duck.processes.clear_processes_data
    :summary:
    ```
* - {py:obj}`delete_process_data <duck.processes.delete_process_data>`
  - ```{autodocx-docstring} duck.processes.delete_process_data
    :summary:
    ```
* - {py:obj}`get_all_processes_data <duck.processes.get_all_processes_data>`
  - ```{autodocx-docstring} duck.processes.get_all_processes_data
    :summary:
    ```
* - {py:obj}`get_process_data <duck.processes.get_process_data>`
  - ```{autodocx-docstring} duck.processes.get_process_data
    :summary:
    ```
* - {py:obj}`set_process_data <duck.processes.set_process_data>`
  - ```{autodocx-docstring} duck.processes.set_process_data
    :summary:
    ```
````

### Data

````{list-table}
:class: autosummary longtable
:align: left

* - {py:obj}`BASE_DIR <duck.processes.BASE_DIR>`
  - ```{autodocx-docstring} duck.processes.BASE_DIR
    :summary:
    ```
* - {py:obj}`FILENAME <duck.processes.FILENAME>`
  - ```{autodocx-docstring} duck.processes.FILENAME
    :summary:
    ```
````

### API

````{py:data} BASE_DIR
:canonical: duck.processes.BASE_DIR
:value: >
   'rstrip(...)'

```{autodocx-docstring} duck.processes.BASE_DIR
```

````

````{py:data} FILENAME
:canonical: duck.processes.FILENAME
:value: >
   '.processes.json'

```{autodocx-docstring} duck.processes.FILENAME
```

````

````{py:function} clear_processes_data()
:canonical: duck.processes.clear_processes_data

```{autodocx-docstring} duck.processes.clear_processes_data
```
````

````{py:function} delete_process_data(name: str) -> dict
:canonical: duck.processes.delete_process_data

```{autodocx-docstring} duck.processes.delete_process_data
```
````

````{py:function} get_all_processes_data() -> dict
:canonical: duck.processes.get_all_processes_data

```{autodocx-docstring} duck.processes.get_all_processes_data
```
````

````{py:function} get_process_data(name) -> dict
:canonical: duck.processes.get_process_data

```{autodocx-docstring} duck.processes.get_process_data
```
````

````{py:function} set_process_data(name: str, data: dict, clear_existing_data=False) -> str
:canonical: duck.processes.set_process_data

```{autodocx-docstring} duck.processes.set_process_data
```
````
